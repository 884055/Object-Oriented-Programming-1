package it.unive.dais.po1.vehicles.animals;

public class HorseCart extends AnimalCart {
    public HorseCart(double initialSpeed, double initialLoad) {
        super(initialSpeed, initialLoad);
    }
}
